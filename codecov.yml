coverage:
  status:
    project:
      default:
        threshold: 0.5%    # allow up to 0.5% drop without failing
    patch:
      default:
        target: auto     # compare patch coverage to base, not project total
        threshold: 0.5%

ignore:
  - "docs/**"
