name: crow_env
channels:
  - conda-forge
dependencies:
  - black
  - clmm
  - hypothesis
  - isort
  - numcosmo >= 0.21.1
  - numpy >= 2.0
  - pip >= 20.1
  - pyccl >= 2.8.0
  - pytest
  - python >= 3.11
  - scipy >= 1.12
